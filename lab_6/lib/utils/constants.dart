import 'package:flutter/material.dart';

class AppColors {
  static const primary     = Color(0xFF1DB954); // Spotify Green
  static const background  = Color(0xFF191414); // Dark Background
  static const surface     = Color(0xFF282828); // Card Background
  static const white       = Color(0xFFFFFFFF);
  static const grey        = Color(0xFFB3B3B3);
  static const darkGrey    = Color(0xFF535353);
}

class AppStrings {
  static const appName     = 'Music Player';
  static const noSongs     = 'No songs found';
  static const noPlaylists = 'No playlists yet';
  static const loading     = 'Loading...';
}