import 'package:flutter/material.dart';

class AppColors {
  // Sunny
  static const sunnyPrimary    = Color(0xFFFDB813);
  static const sunnyBg         = Color(0xFF87CEEB);

  // Rainy
  static const rainyPrimary    = Color(0xFF4A5568);
  static const rainyBg         = Color(0xFF718096);

  // Cloudy
  static const cloudyPrimary   = Color(0xFFA0AEC0);
  static const cloudyBg        = Color(0xFFCBD5E0);

  // Night
  static const nightPrimary    = Color(0xFF2D3748);
  static const nightBg         = Color(0xFF1A202C);

  // General
  static const white           = Color(0xFFFFFFFF);
  static const white70         = Color(0xB3FFFFFF);
  static const white54         = Color(0x8AFFFFFF);
}

class AppStrings {
  static const appName         = 'Weather App';
  static const searchHint      = 'Search city...';
  static const noData          = 'No weather data';
  static const offlineMode     = 'Offline - Showing cached data';
  static const loading         = 'Loading...';
}